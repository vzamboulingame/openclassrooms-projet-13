/**
 * Hero component.
 *
 * @param {void}
 * @returns {JSX.Element} - Rendered component.
 */
export default function Hero() {
  return (
    <div className="hero">
      <div className="hero-content">
        <p class="hero-content-subtitle">No fees.</p>
        <p class="hero-content-subtitle">No minimum deposit.</p>
        <p class="hero-content-subtitle">High interest rates.</p>
        <p class="hero-content-text">
          Open a savings account with Argent Bank today!
        </p>
      </div>
    </div>
  );
}
